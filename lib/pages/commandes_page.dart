import 'package:flutter/material.dart';

class CommandesPage extends StatelessWidget {
  const CommandesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Commandes Page'),
      ),
    );
  }
}